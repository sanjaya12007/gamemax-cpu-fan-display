<Window x:Class="GameMax_CPU_Fan_Display.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About"
        Width="420" Height="220"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="#DC1a1a1a"
        Foreground="White"
        MouseDown="Window_MouseDown">
    <Grid Margin="15">
        <TextBlock Text="About"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>
        <Button x:Name="BtnClose"
                Content="✕"
                Width="32" Height="32"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="False">
                            <Setter Property="Foreground" Value="#CCCCCC"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
            <Button.FontSize>16</Button.FontSize>
            <Button.Background>Transparent</Button.Background>
            <Button.BorderThickness>0</Button.BorderThickness>
        </Button>
        <StackPanel Margin="0,50,0,0">
            <TextBlock Text="GameMax CPU Fan Display"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            <TextBlock FontSize="14" Margin="0,0,0,0">
                <Run Text="Author: "/>
                <Hyperlink NavigateUri="https://github.com/lovenek0"
                           RequestNavigate="Hyperlink_RequestNavigate"
                           Foreground="#4aa3ff">
                    @lovenek0 (Leesa Fomina)
                </Hyperlink>
            </TextBlock>
            <TextBlock Text="Version: 12.09.2025"
                       FontSize="14"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Simple tool for displaying CPU temperature"
                       FontSize="13"
                       Foreground="#bbb"
                       Margin="0,0,0,15"
                       TextWrapping="Wrap"/>
            <TextBlock FontSize="14" HorizontalAlignment="Center">
                <Hyperlink NavigateUri="https://github.com/lovenek0/gamemax-cpu-fan-display"
                           RequestNavigate="Hyperlink_RequestNavigate"
                           Foreground="#4aa3ff">
                    Project on GitHub
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
